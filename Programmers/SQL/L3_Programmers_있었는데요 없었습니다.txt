SELECT A.ANIMAL_ID, A.NAME #A테이블에서 ANIMAL_ID와 NAME을 가져온다
FROM ANIMAL_OUTS A, ANIMAL_INS B #ANIMAL_OUTS 테이블을 A라고 부르고, ANIMAL_INS 테이블을 B라고 부른다
WHERE A.ANIMAL_ID = B.ANIMAL_ID AND A.DATETIME < B.DATETIME #A와B의 테이블의 ID가 같고 A의 DATETIME이 B의 DATETIME보다 작을 때
ORDER BY B.DATETIME ASC; #DATETIME을 기준으로 오름차순 정렬